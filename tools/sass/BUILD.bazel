package(default_visibility = ["//visibility:public"])

load("@build_bazel_rules_nodejs//:defs.bzl", "nodejs_binary")

nodejs_binary(
    name = "sass",
    node_modules = "@sass_runtime_deps//:node_modules",
    entry_point = "sass/sass.js",
)
